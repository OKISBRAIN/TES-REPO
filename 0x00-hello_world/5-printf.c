#include <stdio.h>

/**
 * main - main function is to print a sentence as it is in a new line
 * Return: always 0
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
